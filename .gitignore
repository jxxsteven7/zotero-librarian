# credentials
.env
# generated locally (library content, full texts, staged downloads, audit log) — never committed
cache/
inbox/
logs/
__pycache__/
*.pyc
# terminal recordings behind the README GIFs (docs/demo.sh record); only the GIFs are committed
docs/*.cast
# archify diagrams (docs/archify/*.workflow.json + rendered HTML) — generated locally, not committed
docs/archify/
