# 复制为 .env（Linux/macOS 再 chmod 600）。.env 已在 .gitignore 里，永远不要提交。每台机器各填各的。
ZOTERO_API_KEY=在 https://www.zotero.org/settings/keys 新建，勾选个人库 读/写 + 文件访问
ZOTERO_LIBRARY_ID=14568484
# Zotero 数据目录：不填会自动读本机 Zotero 的 prefs.js（extensions.zotero.dataDir），再不行用平台默认（~/Zotero 或 C:\Users\你\Zotero）。
# 自动探测不对时手填：Zotero 编辑→设置→高级→文件和文件夹 里看。Windows 直接写 C:\Users\你\Zotero，不用转义。
#ZOTERO_DATA_DIR=~/Documents/Zotero
# pdftotext 不在 PATH 里时手指路径（Windows 例：C:\Program Files\poppler-24.08.0\Library\bin\pdftotext.exe）
#PDFTOTEXT=
