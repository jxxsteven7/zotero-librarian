---
name: Taxonomy for another field
about: Share a taxonomy.toml for a field other than robot learning
labels: taxonomy
---

**Field** and the collections / tag families it uses

**Library it was measured on** (number of items, how they were tagged)

**`python3 tools/eval_classify.py` output** (collection accuracy, tag precision / recall)

```
(paste here)
```

Attach or link the `.toml`; a pull request adding it as `taxonomies/<field>.toml` is even better.
